version: "3"

services:
  household-replacement-tracker:
    image: ghcr.io/cristianbisca/householdreplacementtracker:latest
    pull_policy: always
    container_name: household-replacement-tracker
    ports:
      - "3000:3000"
    volumes:
      - ./data:/app/data
    restart: unless-stopped