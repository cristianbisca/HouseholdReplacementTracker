version: "3"

services:
  household-replacement-tracker:
    build: .
    container_name: household-replacement-tracker
    ports:
      - "3000:3000"
    volumes:
      - ./data:/app/data
    restart: unless-stopped
